### 记一次JS报错查询

#### 自我审视，查找不足

    初始由报错信息知道报错是因为global问题，但是由于内心的不信任这种报错，没有仔细探究这个这个问题，转而继续在报错信息里寻找其他细节，未果。甚至最笨的方法都用上里。
把正确的文件和错误的文件对比，由于是打包压缩出来的，代码不好做对比，大致看出来是因为依赖的某个文件有变化导致的。其实这个时候如果注意到global，直接在文件中寻找，是可以立刻找出问题。
最后有点自暴自弃，把不可能的方法也用上了，用正确的源文件替换出错的文件，还是不对。**这里反映出自己不够理性，明知道这种方法是错误的，还要尝试**
这个时候，没有思绪，如果再回头看，再次审查报错信息，请其他同事帮忙，终于确认就是global导致的问题。然后就在源码里查找，很快定位里。
很心痛，定位问题花费2个多小时。主要原因在我看来还是不够细致，细致，细致。
